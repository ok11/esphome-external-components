# ESPHome External Components - Environment Configuration
# Copy this file to .env and customize for your local environment

# Path to ESPHome installation (can be absolute or relative to this project)
# Examples:
#   Absolute: /Users/username/esphome
#   Relative: ../esphome
ESPHOME_PATH=../esphome

# Optional: Path to PlatformIO libraries (auto-detected from ESPHOME_PATH if not set)
# Example: ${ESPHOME_PATH}/.pio/libdeps/esp8266-arduino
# PLATFORMIO_LIBDEPS_PATH=../esphome/.pio/libdeps/esp8266-arduino
